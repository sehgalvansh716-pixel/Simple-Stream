<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:background="@android:color/transparent"
    android:layout_height="match_parent">

    <com.lagradost.cloudstream3.utils.PercentageCropImageView
        android:id="@+id/home_scroll_preview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="matrix"
        app:cropYCenterOffsetPct="0.05"
        app:enableBottomFade="true"
        tools:src="@drawable/example_poster" />

    <!-- Top vignette for brand icons and status bar -->
    <View
        android:id="@+id/title_shadow_top"
        android:layout_width="match_parent"
        android:layout_height="140dp"
        android:layout_gravity="top"
        android:background="@drawable/bg_mobile_hero_top_gradient" />

    <!-- Bottom shadow kept transparent so no black gradient cuts off at the transition boundary -->
    <View
        android:id="@+id/title_shadow_bottom"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:visibility="gone"
        android:background="@android:color/transparent" />

    <!-- Legacy title_shadow kept for ViewBinding compatibility -->
    <View
        android:id="@+id/title_shadow"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:visibility="gone" />

    <!-- Compatibility holder for ViewBinding -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/home_preview_logo"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:contentDescription="@null" />

        <TextView
            android:id="@+id/home_scroll_preview_title"
            android:layout_width="0dp"
            android:layout_height="0dp" />

        <TextView
            android:id="@+id/home_scroll_preview_tags"
            android:layout_width="0dp"
            android:layout_height="0dp" />
    </LinearLayout>

</FrameLayout>